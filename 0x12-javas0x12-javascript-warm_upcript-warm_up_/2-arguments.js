#!/usr/bin/node
const argSize = process.argv;
if (argSize.length === 2) {
  console.log('No argument');
} else if (argSize.length === 3) {
  console.log('Argument found');
} else {
  console.log('Argument found');
}
